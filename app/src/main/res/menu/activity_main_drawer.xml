<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_recently_battle"
            android:icon="@drawable/ic_menu_games"
            android:checked="true"
            android:title="@string/recently_battle" />
        <item
            android:id="@+id/nav_persion_info"
            android:icon="@drawable/ic_menu_person"
            android:title="@string/persion_info" />
        <item
            android:id="@+id/nav_search_battle"
            android:icon="@drawable/ic_menu_search"
            android:title="@string/search_battle" />
        <item
            android:id="@+id/nav_change_binding"
            android:icon="@drawable/ic_menu_change"
            android:title="@string/change_binding" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
